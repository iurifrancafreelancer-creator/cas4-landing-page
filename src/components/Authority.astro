---
---
<section class="py-16 md:py-20 bg-white" id="diferenciais">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8 mb-16 md:mb-24 pb-12 border-b border-gray-100" id="stats-container">
      <div class="text-center">
        <span class="text-3xl md:text-5xl font-extrabold text-[#0A2F5C] mb-1">
          <span class="counter" data-target="800" data-prefix="+" data-suffix="">0</span>
        </span>
        <p class="text-[10px] md:text-sm text-slate-500 font-bold uppercase tracking-wide">Clientes Satisfeitos</p>
      </div>
      <div class="text-center">
        <span class="text-3xl md:text-5xl font-extrabold text-[#0A2F5C] mb-1">
          <span class="counter" data-target="30" data-prefix="+" data-suffix="k">0</span>
        </span>
        <p class="text-[10px] md:text-sm text-slate-500 font-bold uppercase tracking-wide">M² Instalados</p>
      </div>
      <div class="text-center">
        <span class="text-3xl md:text-5xl font-extrabold text-[#0A2F5C] mb-1">
          <span class="counter" data-target="1500" data-prefix="+" data-suffix="">0</span>
        </span>
        <p class="text-[10px] md:text-sm text-slate-500 font-bold uppercase tracking-wide">Pedidos Entregues</p>
      </div>
      <div class="text-center">
        <span class="text-3xl md:text-5xl font-extrabold text-[#0A2F5C] mb-1">
          <span class="counter" data-target="4" data-prefix="" data-suffix=" Anos">0</span>
        </span>
        <p class="text-[10px] md:text-sm text-slate-500 font-bold uppercase tracking-wide">De Excelência</p>
      </div>
    </div>

    <div class="max-w-3xl mx-auto mb-10 md:mb-14 px-4 sm:px-0 text-left md:text-center">
      <h2 class="text-[20px] md:text-4xl font-extrabold text-[#0A2F5C] mb-4 md:mb-6 leading-snug">
        Sabemos que você teme os percalços de uma obra: demora, sujeira, profissionais desqualificados...
      </h2>
      
      <p class="text-[14px] md:text-lg text-slate-600 mb-6 leading-relaxed">
        Chegar na fase de acabamento e lidar com atrasos, sujeira e serviços mal feitos é frustrante. Afinal, é o seu sonho que está em jogo! O seu projeto merece um fechamento perfeito.
      </p>
      
      <p class="text-[14px] md:text-lg text-slate-600 border-l-4 border-[#B28D42] pl-4 italic md:mx-auto md:max-w-2xl text-left">
        "A CAS4 não vende apenas pisos. Nós entregamos capacidade técnica, estética impecável e um serviço de instalação que respeita o seu tempo e valoriza o seu sonho."
      </p>
    </div>
      
      <div class="bg-slate-50 py-8 md:p-10 rounded-2xl shadow-sm border border-slate-100 overflow-hidden w-full">
        <h3 class="text-xl md:text-2xl font-bold text-[#0A2F5C] mb-6 px-6 md:px-0">
          Por que clientes exigentes escolhem a Cas4?
        </h3>
        
        <div class="flex md:flex-col overflow-x-auto snap-x snap-mandatory gap-4 md:gap-8 pb-4 md:pb-0 px-6 md:px-0 hide-scrollbar" style="scrollbar-width: none;">
          
          <div class="snap-center shrink-0 w-[85%] md:w-auto flex flex-col md:flex-row items-start bg-white md:bg-transparent p-6 md:p-0 rounded-xl border border-slate-100 md:border-none shadow-sm md:shadow-none">
            <span class="flex-shrink-0 w-12 h-12 md:w-8 md:h-8 flex items-center justify-center rounded-full bg-[#0A2F5C] text-[#B28D42] font-bold mb-4 md:mb-0 md:mr-4 mt-1">✓</span>
            <div>
              <span class="font-bold text-slate-800 text-lg md:text-base">Venda consultiva</span>
              <p class="text-slate-600 text-sm mt-2 md:mt-1 leading-relaxed">
                Orientação técnica de especialistas para você investir no material correto para a sua necessidade, garantindo estética e durabilidade.
              </p>
            </div>
          </div>

          <div class="snap-center shrink-0 w-[85%] md:w-auto flex flex-col md:flex-row items-start bg-white md:bg-transparent p-6 md:p-0 rounded-xl border border-slate-100 md:border-none shadow-sm md:shadow-none relative">
            <span class="flex-shrink-0 w-12 h-12 md:w-8 md:h-8 flex items-center justify-center rounded-full bg-[#0A2F5C] text-[#B28D42] font-bold mb-4 md:mb-0 md:mr-4 mt-1">✓</span>
            <div>
              <span class="font-bold text-slate-800 text-lg md:text-base">Orçamento 360º (sem surpresas)</span>
              <p class="text-slate-600 text-sm mt-2 md:mt-1 leading-relaxed">
                Esqueça os custos ocultos. Orçamos o piso, todos os insumos necessários e a mão de obra. Transparência total para você não ter surpresas na ponta do lápis.
            </div>
          </div>

          <div class="snap-center shrink-0 w-[85%] md:w-auto flex flex-col md:flex-row items-start bg-white md:bg-transparent p-6 md:p-0 rounded-xl border border-slate-100 md:border-none shadow-sm md:shadow-none">
            <span class="flex-shrink-0 w-12 h-12 md:w-8 md:h-8 flex items-center justify-center rounded-full bg-[#0A2F5C] text-[#B28D42] font-bold mb-4 md:mb-0 md:mr-4 mt-1">✓</span>
            <div>
              <span class="font-bold text-slate-800 text-lg md:text-base">Mão de obra especializada</span>
              <p class="text-slate-600 text-sm mt-2 md:mt-1 leading-relaxed">
                Sabemos como é desgastante buscar e gerenciar prestadores de serviço. Entregamos a solução completa com equipe pontual, educada e extremamente caprichosa.
              </p>
            </div>
          </div>

          <div class="snap-center shrink-0 w-[85%] md:w-auto flex flex-col md:flex-row items-start bg-white md:bg-transparent p-6 md:p-0 rounded-xl border border-slate-100 md:border-none shadow-sm md:shadow-none">
            <span class="flex-shrink-0 w-12 h-12 md:w-8 md:h-8 flex items-center justify-center rounded-full bg-[#0A2F5C] text-[#B28D42] font-bold mb-4 md:mb-0 md:mr-4 mt-1">✓</span>
            <div>
              <span class="font-bold text-slate-800 text-lg md:text-base">Obra limpa e rápida</span>
              <p class="text-slate-600 text-sm mt-2 md:mt-1 leading-relaxed">
                Instalação silenciosa e sem entulho pela casa. Entramos no seu ambiente apenas quando falta o retoque final da pintura.
              </p>
            </div>
          </div>

          <div class="snap-center shrink-0 w-[85%] md:w-auto flex flex-col md:flex-row items-start bg-white md:bg-transparent p-6 md:p-0 rounded-xl border border-slate-100 md:border-none shadow-sm md:shadow-none relative">
            <span class="flex-shrink-0 w-12 h-12 md:w-8 md:h-8 flex items-center justify-center rounded-full bg-[#0A2F5C] text-[#B28D42] font-bold mb-4 md:mb-0 md:mr-4 mt-1">✓</span>
            <div>
              <span class="font-bold text-slate-800 text-lg md:text-base">Assistência pós venda</span>
              <p class="text-slate-600 text-sm mt-2 md:mt-1 leading-relaxed">
                Aqui você não é um cifrão ambulante, nos preocupamos com a qualidade do trabalho e do seu bem estar ao longo do processo. Estamos com você do começo ao fim.
              </p>
            </div>
          </div>

        </div>
      </div>

  </div>
</section>

<script>
  const animateCounter = (counter) => {
    const target = +counter.getAttribute('data-target');
    const prefix = counter.getAttribute('data-prefix');
    const suffix = counter.getAttribute('data-suffix');
    const duration = 2000; // Duração da animação em milissegundos (2 segundos)
    
    let startTimestamp = null;
    
    const step = (timestamp) => {
      if (!startTimestamp) startTimestamp = timestamp;
      const progress = Math.min((timestamp - startTimestamp) / duration, 1);
      
      // Efeito de desaceleração (ease-out)
      const easeOutProgress = 1 - Math.pow(1 - progress, 3);
      let currentVal = Math.floor(easeOutProgress * target);
      
      let formattedVal = currentVal;
      // Adiciona o ponto para milhares (ex: 1500 vira 1.500)
      if (target >= 1000) {
        formattedVal = currentVal.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".");
      }
      
      counter.innerText = `${prefix}${formattedVal}${suffix}`;
      
      if (progress < 1) {
        window.requestAnimationFrame(step);
      } else {
         // Garante que o valor final seja exato
         let finalVal = target >= 1000 ? target.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") : target;
         counter.innerText = `${prefix}${finalVal}${suffix}`;
      }
    };
    
    window.requestAnimationFrame(step);
  };

  // O Intersection Observer garante que a animação só comece quando o usuário rolar a tela até os números
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const counters = entry.target.querySelectorAll('.counter');
        counters.forEach(counter => animateCounter(counter));
        observer.unobserve(entry.target); // Para a observação após animar
      }
    });
  }, { threshold: 0.3 }); // Inicia quando 30% da seção estiver visível

  const statsContainer = document.getElementById('stats-container');
  if(statsContainer) {
    observer.observe(statsContainer);
  }
</script>